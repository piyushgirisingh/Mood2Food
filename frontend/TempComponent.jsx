// Temporary React component for frontend folder
import React from 'react';

const TempComponent = () => {
    return (
        <div>
            This is a temporary React component in frontend.
        </div>
    );
};

export default TempComponent; 